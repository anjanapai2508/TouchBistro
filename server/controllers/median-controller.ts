import {Request, Response} from "express";
module.exports = (req:Request, res:Response, upperLimit: number) => {

    // Logic to find the median of the prime numbers sent as input 
    // input : array of prime numbers : primeNums[]
    // output : single median or median [2]


    res.send("This function should give the median from the array of prime numbers")
};